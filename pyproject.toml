[project]
name = "learning-notes"
version = "0.1.0"
description = "A personal learning-notes site built with MkDocs Material"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material>=9.7.7",
]
